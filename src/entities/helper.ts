export default class Helper {
  constructor() {}

  public resolve() {
    console.log(
      "Replace the address with one queries directly from the graph. Warn about override if there's already a value in this.addess"
    );
  }
}
