import networks, { chains } from "./networks";
import * as globals from "./globals";

export { globals, networks, chains };

const constants = {
  globals,
  networks,
  chains,
};

export default constants;
